# Given a list of numbers, print their sum. When I say given something,
# just make something up and store it in a variable.
"""a = range(100)
b = sum(a)
print(b)

#Given a list of numbers, print the largest of the numbers.
elements = [2, 5, 30, 15, 100, 82, 3]
elements.sort()
print(elements[-1])

#Given a list of numbers, print the smallest of the numbers.
element = [3, 6, 12, 24, 1, 13, 40, 100]
element.sort()
print(element[0])

#Given a list of numbers, print each number in the list that is even.
elements = [2, 9, 10, 55, 22, 26, 18, 45, 192, 87]
for even in elements:
    if even % 2 == 0:
        print(even)

#Given a list of numbers, print each number in the list that is greater than zero.
elements = [-10, -6, 1, 9, -3, 15, 24, 247, -192]
for greater in elements:
    if greater > 0:
         print(greater)

#Given a list of numbers, create a new list which 
#contains every number in the given list which is positive

elements = [27, -12, -72, 8, 16, 42, -1, 1]
more_than_zero = []
for positive in elements:
    if positive > 0:
        more_than_zero.append(positive)
print(more_than_zero)

#Given a list of numbers, and a single factor (also a number), 
#create a new list consisting of each of the numbers
#in the first list multiplied by the factor. Print this list.
element = [2, 6, 8, -45, 32, -12, 88]
factor = 3
result = [item * factor for item in element]
print(result)"""
